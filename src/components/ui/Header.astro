---
interface Props {}

import { supabase } from "../../lib/supabase";
import HeaderCounter from "../islands/HeaderCounter";

const accessToken = Astro.cookies.get("sb-access-token")?.value;
let user = null;

if (accessToken) {
  try {
    const { data } = await supabase.auth.getUser(accessToken);
    if (data?.user) {
      user = data.user;
    }
  } catch (error) {
    // Token inválido
  }
}

const userName =
  user?.user_metadata?.name || user?.email?.split("@")[0] || "Usuario";
---

<!-- Marquee Superior -->
<div
  class="bg-brand-black text-white py-2 text-center overflow-hidden border-b border-white/5"
>
  <div class="animate-marquee whitespace-nowrap inline-block">
    <span class="text-[9px] font-bold uppercase tracking-[0.2em] mx-12"
      >Envío gratuito +50€</span
    >
    <span class="text-[9px] font-bold uppercase tracking-[0.2em] mx-12"
      >Nueva Colección Otoño/Invierno 2026</span
    >
    <span class="text-[9px] font-bold uppercase tracking-[0.2em] mx-12"
      >Descuento exclusivo 10% código: WELCOME10</span
    >
  </div>
</div>

<header
  id="main-header"
  class="sticky top-0 z-[9999] bg-white/90 backdrop-blur-xl border-b border-gray-100 transition-all duration-300"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16 lg:h-20">
      <!-- Navegación Escritorio (Izquierda) -->
      <nav class="hidden lg:flex items-center gap-8 flex-1">
        <a
          href="/"
          class="text-xs font-black uppercase tracking-widest text-slate-900 hover:text-brand-gold transition-colors relative group"
        >
          Inicio
          <span
            class="absolute -bottom-1 left-0 w-0 h-px bg-brand-gold transition-all group-hover:w-full"
          ></span>
        </a>

        <!-- Trigger Tienda PC -->
        <div class="relative group/tienda">
          <button
            type="button"
            class="nav-trigger text-xs font-black uppercase tracking-widest text-slate-900 hover:text-brand-gold transition-all flex items-center gap-1.5 focus:outline-none cursor-pointer"
            data-trigger="tienda"
          >
            Tienda
            <svg
              class="nav-arrow w-3 h-3 transition-transform pointer-events-none"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              ><path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2.5"
                d="M19 9l-7 7-7-7"></path></svg
            >
          </button>

          <!-- Mega Menú Desktop Restaurado -->
          <div
            class="nav-dropdown hidden absolute left-0 top-full mt-2 w-[900px] -ml-[100px] rounded-3xl bg-white shadow-2xl z-[10000] p-10 border border-gray-50 animate-slide-up"
            data-menu="tienda"
          >
            <div class="grid grid-cols-3 gap-12 text-left">
              <!-- ROPA -->
              <div>
                <p
                  class="text-[10px] font-black uppercase tracking-[0.2em] text-brand-gold mb-6 border-b border-brand-gold/10 pb-2"
                >
                  Ropa
                </p>
                <div class="space-y-3">
                  {
                    [
                      "Camisetas",
                      "Polos",
                      "Camisas",
                      "Sudaderas",
                      "Jerseys",
                      "Chaquetas",
                      "Abrigos",
                      "Pantalones",
                    ].map((item) => (
                      <a
                        href="/catalogo/ropa"
                        class="block text-sm text-slate-500 hover:text-brand-black transition-colors"
                      >
                        {item}
                      </a>
                    ))
                  }
                </div>
              </div>
              <!-- CALZADO -->
              <div>
                <p
                  class="text-[10px] font-black uppercase tracking-[0.2em] text-brand-gold mb-6 border-b border-brand-gold/10 pb-2"
                >
                  Calzado
                </p>
                <div class="space-y-3">
                  {
                    [
                      "Zapatillas",
                      "Zapatos casual",
                      "Zapatos vestir",
                      "Botines",
                      "Sandalias",
                    ].map((item) => (
                      <a
                        href="/catalogo/calzado"
                        class="block text-sm text-slate-500 hover:text-brand-black transition-colors"
                      >
                        {item}
                      </a>
                    ))
                  }
                </div>
                <div
                  class="mt-10 p-5 rounded-2xl bg-slate-50 border border-slate-100"
                >
                  <p
                    class="text-[10px] font-bold text-slate-900 uppercase tracking-widest mb-2 italic"
                  >
                    PRO-TIP
                  </p>
                  <p class="text-xs text-slate-500 leading-relaxed">
                    Pruébate nuestro calzado en casa. Devoluciones gratuitas en
                    30 días.
                  </p>
                </div>
              </div>
              <!-- COMPLEMENTOS -->
              <div>
                <p
                  class="text-[10px] font-black uppercase tracking-[0.2em] text-brand-gold mb-6 border-b border-brand-gold/10 pb-2"
                >
                  Complementos
                </p>
                <div class="space-y-3">
                  {
                    [
                      "Gorras y gorros",
                      "Bolsos y mochilas",
                      "Cinturones",
                      "Gafas",
                      "Relojería",
                    ].map((item) => (
                      <a
                        href="/catalogo/complementos"
                        class="block text-sm text-slate-500 hover:text-brand-black transition-colors"
                      >
                        {item}
                      </a>
                    ))
                  }
                </div>
                <div class="mt-8">
                  <a
                    href="/catalogo"
                    class="flex items-center justify-between p-4 rounded-xl bg-brand-black text-white hover:bg-brand-gold transition-all group/all"
                  >
                    <span
                      class="text-[10px] font-bold uppercase tracking-widest"
                      >Colección Completa</span
                    >
                    <svg
                      class="w-4 h-4 group-hover/all:translate-x-1 transition-transform"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                      ><path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M13 7l5 5m0 0l-5 5m5-5H6"></path></svg
                    >
                  </a>
                </div>
              </div>
            </div>
          </div>
        </div>

        <a
          href="/ofertas"
          class="text-xs font-black uppercase tracking-widest text-red-500 hover:text-red-600 transition-colors flex items-center gap-2 group"
        >
          Ofertas <span
            class="flex h-2 w-2 rounded-full bg-red-500 animate-pulse"></span>
        </a>
      </nav>

      <!-- Logo Central -->
      <div class="flex-shrink-0 flex items-center justify-center">
        <a
          href="/"
          class="text-xl lg:text-3xl font-serif font-black tracking-tighter hover:scale-[1.02] transition-transform"
        >
          <span class="text-slate-900">Fashion</span><span
            class="text-brand-gold ml-1">Store</span
          >
        </a>
      </div>

      <!-- Acciones Derecha -->
      <div class="flex-1 flex items-center justify-end gap-1 sm:gap-4">
        <!-- Buscador PC -->
        <div class="relative hidden lg:block">
          <div
            class="flex items-center h-10 px-4 bg-slate-50 border border-slate-100 rounded-full w-44 lg:w-60 focus-within:bg-white transition-all shadow-sm"
          >
            <svg
              class="w-4 h-4 text-slate-400"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              ><path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg
            >
            <input
              id="search-input"
              type="text"
              placeholder="Buscar..."
              class="w-full ml-2 bg-transparent text-sm focus:outline-none"
            />
          </div>
          <div
            id="search-results"
            class="hidden absolute top-full right-0 mt-4 w-[350px] bg-white rounded-2xl shadow-2xl border border-gray-100 z-[10002] overflow-hidden"
          >
            <div id="search-results-content" class="py-2"></div>
            <div class="bg-slate-50 p-3 text-center border-t border-slate-100">
              <span
                class="text-[10px] text-slate-400 uppercase font-black tracking-widest"
                >Presiona enter para ver más</span
              >
            </div>
          </div>
        </div>

        <div class="flex items-center gap-1 sm:gap-2">
          <!-- Favoritos -->
          <a
            href="/favoritos"
            class="p-2 rounded-full hover:bg-slate-50 relative group transition-colors cursor-pointer"
          >
            <svg
              class="w-5 h-5 text-slate-700 group-hover:text-red-500"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              ><path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="1.8"
                d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"
              ></path></svg
            >
            <HeaderCounter client:load type="favorite" />
          </a>

          <!-- Perfil PC -->
          <div class="relative hidden lg:block">
            {
              user ? (
                <div class="relative">
                  <button
                    id="profile-trigger"
                    class="flex items-center focus:outline-none p-1 relative z-[100] cursor-pointer"
                  >
                    <div class="w-8 h-8 rounded-full bg-slate-200/50 border border-slate-200 flex items-center justify-center hover:border-brand-gold transition-colors pointer-events-none shadow-sm">
                      <span class="text-xs font-black text-slate-800 uppercase">
                        {userName.charAt(0)}
                      </span>
                    </div>
                    <div class="pointer-events-none">
                      <HeaderCounter client:load type="notification" />
                    </div>
                  </button>
                  <div
                    id="profile-dropdown"
                    class="hidden absolute right-0 top-full mt-4 w-64 bg-white rounded-2xl shadow-2xl border border-slate-100 py-4 z-[10001] animate-slide-up"
                  >
                    <div class="px-6 py-4 border-b border-gray-50 mb-4">
                      <p class="text-[10px] text-slate-400 font-bold uppercase tracking-widest mb-1 leading-none">
                        TU CUENTA
                      </p>
                      <p class="text-base font-bold text-slate-800 truncate">
                        {userName}
                      </p>
                    </div>
                    <div class="space-y-1">
                      <a
                        href="/mi-cuenta"
                        class="flex items-center px-6 py-2.5 text-sm text-slate-600 hover:bg-slate-50 hover:text-brand-black transition-colors font-medium"
                      >
                        Perfil
                      </a>
                      <a
                        href="/mensajes"
                        class="flex items-center justify-between px-6 py-2.5 text-sm text-slate-600 hover:bg-slate-50 hover:text-brand-black transition-colors font-medium"
                      >
                        Mis Mensajes{" "}
                        <HeaderCounter client:load type="inquiry" />
                      </a>
                      <a
                        href="/mis-pedidos"
                        class="flex items-center px-6 py-2.5 text-sm text-slate-600 hover:bg-slate-50 hover:text-brand-black transition-colors font-medium"
                      >
                        Pedidos
                      </a>
                      <a
                        href="/mis-cupones"
                        class="flex items-center justify-between px-6 py-2.5 text-sm text-slate-600 hover:bg-slate-50 hover:text-brand-black transition-colors font-medium"
                      >
                        Mis Cupones{" "}
                        <HeaderCounter client:load type="coupon_notification" />
                      </a>

                      {user?.app_metadata?.role === "admin" && (
                        <div class="mt-4 pt-4 border-t border-gray-100">
                          <a
                            href="/admin"
                            class="flex items-center px-6 py-2.5 text-[10px] font-black uppercase tracking-widest text-brand-gold hover:text-brand-black"
                          >
                            Admin Panel
                          </a>
                        </div>
                      )}

                      <div class="mt-2 pt-2 border-t border-gray-50 px-2">
                        <button
                          id="logout-button"
                          class="w-full text-left px-4 py-3 text-sm text-red-500 font-bold hover:bg-red-50 rounded-xl transition-colors cursor-pointer"
                        >
                          Cerrar Sesión
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
              ) : (
                <a
                  href="/login"
                  class="px-4 py-2 bg-brand-black text-white text-[10px] font-black uppercase tracking-widest rounded-full hover:bg-brand-gold transition-all cursor-pointer"
                >
                  Login
                </a>
              )
            }
          </div>

          <!-- Carrito -->
          <button
            id="cart-button"
            class="p-2 rounded-full bg-slate-50 hover:bg-brand-gold group transition-all relative cursor-pointer focus:outline-none"
          >
            <svg
              class="w-6 h-6 text-slate-700 group-hover:text-white"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
              ><path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="1.8"
                d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path></svg
            >
            <HeaderCounter client:load type="cart" dark />
          </button>
        </div>
      </div>
    </div>
  </div>
</header>

<!-- BARRA MÓVIL INFERIOR -->
<nav
  class="fixed bottom-0 left-0 right-0 z-[9999] lg:hidden bg-white/90 backdrop-blur-xl border-t border-gray-100 px-6 py-3 shadow-[0_-5px_20px_rgba(0,0,0,0.05)] safe-area-bottom"
>
  <div class="flex items-center justify-between max-w-lg mx-auto">
    <a
      href="/"
      class="flex flex-col items-center gap-1 text-slate-900 active:scale-95 transition-transform"
      ><svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24"
        ><path d="M10 20v-6h4v6h5v-8h3L12 3 2 12h3v8z"></path></svg
      ><span class="text-[9px] font-black uppercase tracking-widest"
        >Inicio</span
      ></a
    >
    <a
      href="/catalogo"
      class="flex flex-col items-center gap-1 text-slate-400 active:scale-95 transition-transform"
      ><svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        stroke-width="2.5"
        ><path d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"></path></svg
      ><span class="text-[9px] font-bold uppercase tracking-widest">Tienda</span
      ></a
    >
    <button
      id="mobile-search-trigger"
      class="flex flex-col items-center gap-1 -mt-8 relative active:scale-90 transition-transform"
      ><div
        class="w-14 h-14 bg-brand-black text-white rounded-full flex items-center justify-center shadow-xl border-4 border-white"
      >
        <svg
          class="w-6 h-6"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
          stroke-width="2.5"
          ><path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
      </div><span
        class="text-[9px] font-black text-brand-black uppercase tracking-widest mt-1"
        >Buscar</span
      ></button
    >
    <a
      href="/ofertas"
      class="flex flex-col items-center gap-1 text-slate-400 active:scale-95 transition-transform"
      ><svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        stroke-width="2.5"
        ><path d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
        ></path></svg
      ><span class="text-[9px] font-bold uppercase tracking-widest"
        >Ofertas</span
      ></a
    >
    <a
      href={user ? "/mi-cuenta" : "/login"}
      class="flex flex-col items-center gap-1 text-slate-400 active:scale-95 transition-transform"
      ><svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
        stroke-width="2.5"
        ><path
          d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
        ></path></svg
      ><span class="text-[9px] font-bold uppercase tracking-widest"
        >{user ? "Cuenta" : "Login"}</span
      ></a
    >
  </div>
</nav>

<!-- BUSCADOR MÓVIL FULL-SCREEN -->
<div
  id="mobile-search-overlay"
  class="fixed inset-0 z-[10000] bg-white translate-y-full transition-transform duration-500 ease-in-out px-6 pt-10"
>
  <div class="flex flex-col h-full max-w-2xl mx-auto">
    <div class="flex items-center justify-between mb-8">
      <span class="text-xs font-black uppercase tracking-widest text-brand-gold"
        >¿Qué buscas hoy?</span
      >
      <button
        id="close-search"
        class="p-2 text-slate-400 hover:text-brand-black transition-colors"
      >
        <svg
          class="w-8 h-8"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
          ><path d="M6 18L18 6M6 6l12 12" stroke-width="2.5"></path></svg
        >
      </button>
    </div>

    <div class="relative mb-8">
      <input
        id="mobile-search-input"
        type="text"
        placeholder="Zapatillas, Polos, Camisas..."
        class="w-full text-2xl font-serif font-black border-b-2 border-slate-100 focus:border-brand-gold bg-transparent py-4 focus:outline-none transition-colors"
        autocomplete="off"
      />
    </div>

    <div
      id="mobile-search-results-content"
      class="flex-1 overflow-y-auto custom-scrollbar space-y-4 pb-10"
    >
      <!-- Tendencias -->
      <div id="mobile-search-trends" class="space-y-6 pt-4">
        <p
          class="text-[10px] font-black uppercase tracking-widest text-slate-400"
        >
          Tendencias actuales
        </p>
        <div class="flex flex-wrap gap-2">
          {
            [
              "Novedades",
              "Best Sellers",
              "Zapatillas",
              "Sudaderas",
              "Outlet",
            ].map((tag) => (
              <button class="px-5 py-2.5 bg-slate-50 rounded-full text-xs font-bold text-slate-600 hover:bg-brand-black hover:text-white transition-all uppercase tracking-tighter shadow-sm">
                {tag}
              </button>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  @keyframes marquee {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }
  .animate-marquee {
    display: inline-block;
    padding-left: 100%;
    animation: marquee 30s linear infinite;
  }
  .animate-slide-up {
    animation: slideUp 0.35s cubic-bezier(0.16, 1, 0.3, 1);
  }
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(12px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .safe-area-bottom {
    padding-bottom: calc(0.75rem + env(safe-area-inset-bottom, 0px));
  }
  .custom-scrollbar::-webkit-scrollbar {
    width: 4px;
  }
  .custom-scrollbar::-webkit-scrollbar-thumb {
    background: #e2e8f0;
    border-radius: 10px;
  }
</style>

<script>
  import { supabase } from "../../lib/supabase";
  import { isCartOpen } from "../../stores/cartStore";

  function handleHeader() {
    // --- Referencias PC ---
    const pTrigger = document.getElementById("profile-trigger");
    const pDropdown = document.getElementById("profile-dropdown");
    const pcInput = document.getElementById("search-input") as HTMLInputElement;
    const pcResults = document.getElementById("search-results");
    const pcContent = document.getElementById("search-results-content");

    // --- Referencias Móvil ---
    const mSearchTrigger = document.getElementById("mobile-search-trigger");
    const mSearchOverlay = document.getElementById("mobile-search-overlay");
    const mSearchClose = document.getElementById("close-search");
    const mInput = document.getElementById(
      "mobile-search-input",
    ) as HTMLInputElement;
    const mResultsContent = document.getElementById(
      "mobile-search-results-content",
    );

    const formatPrice = (cents: number) => {
      return new Intl.NumberFormat("es-ES", {
        style: "currency",
        currency: "EUR",
      }).format(cents / 100);
    };

    const closeAll = () => {
      document
        .querySelectorAll(".nav-dropdown")
        .forEach((d) => d.classList.add("hidden"));
      pDropdown?.classList.add("hidden");
    };

    // LÓGICA DE BÚSQUEDA (Común para PC y Móvil)
    const performSearch = async (
      query: string,
      container: HTMLElement,
      resultsDiv?: HTMLElement,
    ) => {
      if (!query || query.trim().length < 2) {
        if (resultsDiv) resultsDiv.classList.add("hidden");
        return;
      }

      try {
        const response = await fetch(
          `/api/search.json?q=${encodeURIComponent(query)}`,
        );
        const data = await response.json();

        if (!data.success || !data.results || data.results.length === 0) {
          container.innerHTML = `<div class="px-4 py-3 text-sm text-gray-500 text-center">No se encontraron productos</div>`;
        } else {
          container.innerHTML = data.results
            .map(
              (p: any) => `
            <a href="/producto/${p.slug}" class="flex items-center gap-3 px-4 py-3 hover:bg-gray-50 transition-colors border-b border-gray-100 last:border-0">
              <img src="${p.images?.[0] || ""}" class="w-12 h-12 object-cover rounded shadow-sm" />
              <div class="flex-1 min-w-0">
                <p class="text-xs font-bold text-gray-900 leading-tight truncate">${p.name}</p>
                <p class="text-xs text-brand-gold mt-1 font-bold">${formatPrice(p.price)}</p>
              </div>
            </a>
          `,
            )
            .join("");
        }
        if (resultsDiv) resultsDiv.classList.remove("hidden");
      } catch (error) {
        console.error("Search error:", error);
      }
    };

    // Eventos Búsqueda PC
    let pcTimeout: any;
    pcInput?.addEventListener("input", () => {
      clearTimeout(pcTimeout);
      pcTimeout = setTimeout(
        () => performSearch(pcInput.value, pcContent!, pcResults!),
        300,
      );
    });

    // Eventos Búsqueda Móvil
    let mTimeout: any;
    mInput?.addEventListener("input", () => {
      clearTimeout(mTimeout);
      mTimeout = setTimeout(
        () => performSearch(mInput.value, mResultsContent!),
        300,
      );
    });

    // LÓGICA PC: Perfil y Menús
    if (pTrigger) {
      pTrigger.onclick = (e) => {
        e.stopPropagation();
        const isHidden = pDropdown?.classList.contains("hidden");
        closeAll();
        if (isHidden) pDropdown?.classList.remove("hidden");
      };
    }

    document.querySelectorAll(".nav-trigger").forEach((t) => {
      (t as HTMLElement).onclick = (e) => {
        e.stopPropagation();
        const key = t.getAttribute("data-trigger");
        const menu = document.querySelector(`[data-menu="${key}"]`);
        const wasOpen = !menu?.classList.contains("hidden");
        closeAll();
        if (!wasOpen) menu?.classList.remove("hidden");
      };
    });

    // LÓGICA MÓVIL: Buscador Overlay
    mSearchTrigger?.addEventListener("click", () => {
      mSearchOverlay?.classList.remove("translate-y-full");
      document.body.style.overflow = "hidden";
      setTimeout(() => mInput?.focus(), 500);
    });
    mSearchClose?.addEventListener("click", () => {
      mSearchOverlay?.classList.add("translate-y-full");
      document.body.style.overflow = "";
    });

    // Cierre Global y Carrito
    window.onclick = (e) => {
      const target = e.target as Node;
      if (
        pDropdown &&
        !pDropdown.contains(target) &&
        !pTrigger?.contains(target)
      )
        pDropdown.classList.add("hidden");
      if (
        pcResults &&
        !pcResults.contains(target) &&
        !pcInput?.contains(target)
      )
        pcResults.classList.add("hidden");
      document.querySelectorAll(".nav-dropdown").forEach((d) => {
        const tr = document.querySelector(
          `[data-trigger="${d.getAttribute("data-menu")}"]`,
        );
        if (!d.contains(target) && !tr?.contains(target))
          d.classList.add("hidden");
      });
    };

    document
      .getElementById("cart-button")
      ?.addEventListener("click", () => isCartOpen.set(true));
    document
      .getElementById("logout-button")
      ?.addEventListener("click", async () => {
        await supabase.auth.signOut();
        document.cookie = "sb-access-token=; Path=/; Max-Age=0";
        window.location.href = "/";
      });
  }

  document.addEventListener("DOMContentLoaded", handleHeader);
  document.addEventListener("astro:after-swap", handleHeader);
  if (document.readyState !== "loading") handleHeader();
</script>
